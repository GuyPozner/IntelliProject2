This repo contains all the files which are necessary to reproduce the results of the training and the the task in the intellicage:

1. The “Sessions” folder contains each session as produced by the intellicage, containing the experiment file with the relevant animals.
2. The “Log” folder contains the analysis files with the scripts that produced them, everything is embedded in an R markdown file.

